const date: Date = new Date();

const reg: RegExp = new RegExp('.+?');

const myPromise: Promise<number> = Promise.resolve(42);

const elem: HTMLElement = document.createElement('div');

const ulElem: HTMLUListElement = document.createElement('ul');

const map: Map<number, string> = new Map();
const set: Set<number> = new Set();
const lst: NodeList = document.querySelectorAll('div');
const findElem: HTMLCollection = document.getElementsByTagName('div');
